<Window x:Class="Regard3D_Tablet.CreateDensePointcloud"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Densification" Height="600" Width="700"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Background" Value="#E0F7FA"/>
            <Setter Property="Foreground" Value="#0288D1"/>
            <Setter Property="IsSelectionRangeEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Border x:Name="TrackBackground" Height="4" CornerRadius="2" Background="#E1F5FE" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Height="4">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="#0288D1" CornerRadius="2" Height="4"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="18" Height="18" VerticalAlignment="Center">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="#0288D1" Stroke="White" StrokeThickness="0" Width="18" Height="18"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="SemiBold" Foreground="#424242" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OkButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Foreground" Value="#0288D1"/>
            <Setter Property="BorderBrush" Value="#0288D1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E1F5FE"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#B3E5FC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Foreground" Value="#D32F2F"/>
            <Setter Property="BorderBrush" Value="#EF5350"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEBEE"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFCDD2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsReadOnly" Value="True">
                    <Setter Property="Background" Value="#FAFAFA"/>
                    <Setter Property="Foreground" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="5" BorderBrush="#CCCCCC" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20,0">
                <TextBlock Text="Densification" VerticalAlignment="Center" FontSize="18" FontWeight="Normal"/>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center" Width="30" Height="30" Background="Transparent" BorderThickness="0" Content="✕" FontSize="16" Foreground="#757575" Click="CloseButton_Click"/>
            </Grid>

            <Border Grid.Row="0" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" VerticalAlignment="Bottom"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel>
                    <GroupBox Header="Densification method" Style="{StaticResource ModernGroupBox}">
                        <ComboBox Height="35" Padding="10,0" VerticalContentAlignment="Center" SelectedIndex="0" Background="White">
                            <ComboBoxItem Content="CMVS/PMVS"/>
                        </ComboBox>
                    </GroupBox>

                    <GroupBox Header="Parameters for CMVS/PMVS" Style="{StaticResource ModernGroupBox}">
                        <StackPanel>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="260"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Number of threads:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Text="8" HorizontalContentAlignment="Left" Padding="8,5"/>
                                <CheckBox Grid.Row="0" Grid.Column="2" Content="Use visibility information (CMVS)" VerticalAlignment="Center" Margin="20,0,0,0"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Maximum number of images per cluster:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="100" HorizontalContentAlignment="Left" Padding="8,5"/>
                            </Grid>

                            <Border BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Margin="0,0,0,20"/>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Level:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Column="1" Text="{Binding ElementName=SldLevel, Path=Value}" IsReadOnly="True" HorizontalContentAlignment="Center"/>
                                <Slider x:Name="SldLevel" Grid.Column="2" Style="{StaticResource ModernSlider}" Minimum="0" Maximum="10" Value="1" IsSnapToTickEnabled="True" TickFrequency="1" Margin="15,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Cell size:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Column="1" Text="{Binding ElementName=SldCellSize, Path=Value}" IsReadOnly="True" HorizontalContentAlignment="Center"/>
                                <Slider x:Name="SldCellSize" Grid.Column="2" Style="{StaticResource ModernSlider}" Minimum="1" Maximum="10" Value="2" IsSnapToTickEnabled="True" TickFrequency="1" Margin="15,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Threshold:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Column="1" Text="{Binding ElementName=SldThreshold, Path=Value, StringFormat=N1}" IsReadOnly="True" HorizontalContentAlignment="Center"/>
                                <Slider x:Name="SldThreshold" Grid.Column="2" Style="{StaticResource ModernSlider}" Minimum="0" Maximum="1" Value="0.7" IsSnapToTickEnabled="True" TickFrequency="0.1" Margin="15,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="wsize:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Column="1" Text="{Binding ElementName=SldWsize, Path=Value}" IsReadOnly="True" HorizontalContentAlignment="Center"/>
                                <Slider x:Name="SldWsize" Grid.Column="2" Style="{StaticResource ModernSlider}" Minimum="0" Maximum="15" Value="7" IsSnapToTickEnabled="True" TickFrequency="1" Margin="15,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Min. image num:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0"/>
                                <TextBox Grid.Column="1" Text="{Binding ElementName=SldMinImg, Path=Value}" IsReadOnly="True" HorizontalContentAlignment="Center"/>
                                <Slider x:Name="SldMinImg" Grid.Column="2" Style="{StaticResource ModernSlider}" Minimum="1" Maximum="10" Value="3" IsSnapToTickEnabled="True" TickFrequency="1" Margin="15,0,0,0"/>
                            </Grid>

                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="1" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" VerticalAlignment="Bottom"/>

            <Grid Grid.Row="2" Margin="20,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="OK" Style="{StaticResource OkButtonStyle}" Margin="0,0,10,0" Click="CloseButton_Click"/>
                    <Button Content="Cancel" Style="{StaticResource CancelButtonStyle}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>