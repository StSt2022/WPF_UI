<Window x:Class="Regard3D_Mobile.CameraWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Regard3D_Mobile"
        mc:Ignorable="d"
        Title="Camera" Height="380" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="WhiteSmoke"
		FontSize="14">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Camera" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="Black" Margin="10"/>

        <Border Grid.Row="2" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0">
                    <Image Source="/Icons/switch-camera.png" Width="28" Height="28"/>
                </Button>

                <Button Grid.Column="1" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0">
                    <Ellipse Grid.Column="1" Stroke="Gray" StrokeThickness="3" Width="60" Height="60"/>
                </Button>

                <Button Grid.Column="2" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                    <Image Source="/Icons/settings.png" Width="28" Height="28"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>