<Window x:Class="Regard3D_Kids.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Regard3D_Kids"
        mc:Ignorable="d"
        Title="Matching results" Height="700" Width="1000"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="PreviewButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFCA28"/>
            <Setter Property="Foreground" Value="#3E2723"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="#FFA000"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFD54F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFB300"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0,0,0,0"/>
                                <Setter TargetName="Bd" Property="Margin" Value="0,3,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ItemListStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#F3E5F5"/>
            <Setter Property="BorderBrush" Value="#CE93D8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer Focusable="false" VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#CE93D8"/>
            <Setter Property="Foreground" Value="#4A148C"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E1BEE7"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D1C4E9"/>
                                <Setter Property="Foreground" Value="#311B92"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="WindowControlBtn" TargetType="Button">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" CornerRadius="11" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bdr" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.9" ScaleY="0.9" CenterX="11" CenterY="11"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FFFFE1" BorderBrush="#FFEB3B" BorderThickness="4" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#FFF59D" MouseDown="Header_MouseDown">
                <Border CornerRadius="8,8,0,0" Background="#FFF59D"/>
                <TextBlock Text="Matching results" VerticalAlignment="Center" Margin="15,0" FontSize="16" FontWeight="Bold" Foreground="#2962FF"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <Button Style="{StaticResource WindowControlBtn}" Background="#FDD835" Click="MinimizeButton_Click">
                        <Path Data="M0,5 L10,5" Stroke="Black" StrokeThickness="2" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlBtn}" Background="#00E676" Click="MaximizeButton_Click">
                        <Path Data="M1,1 L9,1 L9,9 L1,9 Z" Stroke="Black" StrokeThickness="2" Fill="Transparent" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlBtn}" Background="#FF5252" Click="CloseButton_Click">
                        <Path Data="M0,0 L8,8 M0,8 L8,0" Stroke="Black" StrokeThickness="2" SnapsToDevicePixels="True"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="15" Grid.IsSharedSizeScope="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Background="#F5DEB3" BorderBrush="#F4BB44" BorderThickness="2" CornerRadius="10" Padding="10">
                    <GroupBox Header="Keypoints" FontSize="14" FontWeight="Bold" Foreground="#E65100" BorderThickness="0" Padding="0,5,0,0">
                        <ListBox Style="{StaticResource ItemListStyle}" SelectionMode="Single">
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🦋" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 1 - Lorem ipsum dolor sit amet..." TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🦄" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 2"/>
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🦖" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 3"/>
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem BorderThickness="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="☀️" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 4"/>
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>
                    </GroupBox>
                </Border>

                <Border Grid.Row="2" Grid.Column="0" Background="#F5DEB3" BorderBrush="#F4BB44" BorderThickness="2" CornerRadius="10" Padding="10">
                    <GroupBox BorderThickness="0" Padding="0,5,0,0" FontSize="14">
                        <GroupBox.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Matches" Grid.Column="0" FontSize="14" FontWeight="Bold" Foreground="#E65100" Margin="0, 0, 115, 0"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="Show matches filtered by:" Foreground="#D84315" Margin="0,0,5,0" FontSize="11"/>
                                    <ComboBox SelectedIndex="0" VerticalAlignment="Center" VerticalContentAlignment="Center" FontSize="11">
                                        <ComboBoxItem Content="Fundamental matrix"/>
                                        <ComboBoxItem Content="Matrix 2"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </GroupBox.Header>
                        <ListBox Style="{StaticResource ItemListStyle}">
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🦊" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 1"/>
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🦉" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 2"/>
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem BorderThickness="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🐳" Margin="0,0,8,0" FontSize="18" FontFamily="Segoe UI Emoji"/>
                                    <TextBlock Text="Item 3"/>
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>
                    </GroupBox>
                </Border>

                <Border Grid.Row="0" Grid.Column="2" Background="#FFEBEE" BorderBrush="#EF9A9A" BorderThickness="2" CornerRadius="10" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Background="White" CornerRadius="8">
                            <Grid>
                                <Rectangle Stroke="#CFD8DC" StrokeThickness="2" StrokeDashArray="4 4" RadiusX="8" RadiusY="8"/>
                                <TextBlock Text="🖼️" FontSize="56" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Emoji"/>
                            </Grid>
                        </Border>
                        <Grid Grid.Row="1" Margin="0,10,0,0">
                            <CheckBox Content="Show keypoints" IsChecked="True" VerticalAlignment="Center" Foreground="#C62828" FontWeight="Bold"/>
                            <Button Content="Open preview window" Style="{StaticResource PreviewButtonStyle}" HorizontalAlignment="Right" Click="OpenPreview_Click"/>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Grid.Column="2" Background="#FFEBEE" BorderBrush="#EF9A9A" BorderThickness="2" CornerRadius="10" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Background="White" CornerRadius="8">
                            <Grid>
                                <Rectangle Stroke="#CFD8DC" StrokeThickness="2" StrokeDashArray="4 4" RadiusX="8" RadiusY="8"/>
                                <TextBlock Text="🏞️" FontSize="56" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Emoji"/>
                            </Grid>
                        </Border>
                        <Grid Grid.Row="1" Margin="0,10,0,0">
                            <CheckBox Content="Show matches" IsChecked="True" VerticalAlignment="Center" Foreground="#C62828" FontWeight="Bold"/>
                            <Button Content="Open preview window" Style="{StaticResource PreviewButtonStyle}" HorizontalAlignment="Right" Click="OpenMatchesPreview_Click"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>