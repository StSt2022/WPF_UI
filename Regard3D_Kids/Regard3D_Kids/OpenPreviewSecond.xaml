<Window x:Class="Regard3D_Kids.OpenPreviewSecond"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Matches preview" Height="550" Width="1100"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="ModalButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0,0,0,0"/>
                                <Setter TargetName="Bd" Property="Margin" Value="0,3,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BlueButton" TargetType="Button" BasedOn="{StaticResource ModalButtonStyle}">
            <Setter Property="Background" Value="#42A5F5"/>
            <Setter Property="BorderBrush" Value="#1E88E5"/>
        </Style>

        <Style x:Key="RedButton" TargetType="Button" BasedOn="{StaticResource ModalButtonStyle}">
            <Setter Property="Background" Value="#EF5350"/>
            <Setter Property="BorderBrush" Value="#D32F2F"/>
        </Style>

        <Style x:Key="WindowControlBtn" TargetType="Button">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" CornerRadius="11" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FFFDE7" BorderBrush="#FFEB3B" BorderThickness="4" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#FFF59D">
                <Border CornerRadius="8,8,0,0" Background="#FFF59D"/>
                <TextBlock Text="Matches preview" VerticalAlignment="Center" Margin="15,0" FontSize="16" FontWeight="Bold" Foreground="#2962FF"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <Button Style="{StaticResource WindowControlBtn}" Background="#FDD835">
                        <Path Data="M0,5 L10,5" Stroke="Black" StrokeThickness="2" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlBtn}" Background="#00E676">
                        <Path Data="M1,1 L9,1 L9,9 L1,9 Z" Stroke="Black" StrokeThickness="2" Fill="Transparent" SnapsToDevicePixels="True"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlBtn}" Background="#FF5252" Click="CloseButton_Click">
                        <Path Data="M0,0 L8,8 M0,8 L8,0" Stroke="Black" StrokeThickness="2" SnapsToDevicePixels="True"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="Transparent" Padding="15">
                <Grid>
                    <Rectangle Fill="White" 
                               Stroke="#B0BEC5" 
                               StrokeThickness="2" 
                               StrokeDashArray="5 3" 
                               RadiusX="10" 
                               RadiusY="10"/>

                    <TextBlock Text="🏞️" FontSize="64" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Emoji"/>
                </Grid>
            </Border>

            <Grid Grid.Row="2" Background="#FFE0B2">
                <Border CornerRadius="0,0,8,8" Background="#FFE0B2"/>
                <Grid Margin="15,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                        <ComboBox SelectedIndex="0" Width="130" Height="28" VerticalContentAlignment="Center" Padding="5,0">
                            <ComboBoxItem Content="Rich keypoints"/>
                            <ComboBoxItem Content="Simple keypoints"/>
                        </ComboBox>

                        <CheckBox Content="Show single keypoints" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="13" Foreground="#3E2723"/>
                        <CheckBox Content="Enable track filter" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="13" Foreground="#3E2723"/>

                        <ComboBox SelectedIndex="0" Width="160" Height="28" Margin="15,0,0,0" VerticalContentAlignment="Center" Padding="5,0">
                            <ComboBoxItem Content="Fundamental matches"/>
                            <ComboBoxItem Content="Matrix matches"/>
                        </ComboBox>

                        <TextBlock Text="Zoom: 7.4%" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="14" Foreground="#3E2723" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Button Content="Zoom in" Style="{StaticResource BlueButton}" Margin="0,0,10,0"/>
                        <Button Content="Zoom out" Style="{StaticResource BlueButton}" Margin="0,0,10,0"/>
                        <Button Content="Close" Style="{StaticResource RedButton}" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>